// Dependencies
import { useEffect } from "react";
import { useStore } from "@/stores";

// Enemies manager component
export function EnemiesManager() {
  // Getting the enemies data that the code will need
  //const enemies = useStore("enemies", s => s.enemies);
  
  return null;
}
